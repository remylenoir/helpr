import React, { Fragment } from 'react';
import Onboarding from '../onboarding/Onboarding';

const Landing = () => {
  return (
    <Fragment>
      <Onboarding />
    </Fragment>
  );
};

export default Landing;
